import React from 'react';
import AppRouter from './Router';  // Import the AppRouter component which handles routing

function App() {
  return (
    <div>
      <AppRouter />
    </div>
  );
}

export default App;
